version: '3.8'
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
      
  trading_bot:
    build: .
    env_file:
      - .env
    depends_on:
      - redis
    volumes:
      - ./logs:/app/logs

volumes:
  redis_data:
